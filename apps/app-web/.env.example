# Supabase client config — from Supabase dashboard > Project settings > API.
# Not secret (this is the public anon key, meant to ship in a client
# bundle), protection comes from Row Level Security, but kept out of git
# for per-environment flexibility.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
