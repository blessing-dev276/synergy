# Success stories content

Each file in this folder is one member story (name, status, picture, result,
story), managed through **`/stories-admin`** — not by hand.

This README exists just so the folder isn't empty in git — safe to ignore.
